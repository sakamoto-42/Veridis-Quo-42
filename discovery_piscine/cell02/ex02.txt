cd /home/event/discovery_piscine
mkdir cell02
cd cell02
mkdir ex02
cd ex02

				// RTFM !

man touch			// manuel pour touch
man rm				// manuel pour rm

				// DEBUT CREATION DE DOSSIERS ET FICHIERS POUR TEST

touch test1			// touch nom_du_fichier : crée le fichier nom_du_fichier
				// crée le fichier test1

mkdir test2			// crée le répertoire test2
cd test2			// navigue vers le répertoire test2
touch test3			// crée le fichier test3 dans le répertoire test2
mkdir test4			// crée le répertoire test4 dans le répertoire test2
cd ..				// navigue vers le répertoire parent (dans le répertoire ex02)
touch .test			// crée le fichier caché .test
touch ..test			// crée le fichier caché ..test

				// FIN CREATION DE DOSSIERS ET FICHIERS POUR TEST


man ls		// INDICE : RTFM !
		// INDICE : Ce qui n'est pas demandé n'est pas à faire
		// par défaut, la commande ls n'affiche pas les fichiers cachés

ls -m		// l'option -m sépare la liste des entrées par des virgules

vim midLS
i						
ls -m
echap						
:wq						
enter						
cat midLS								

echo "ls -m" > midLS
cat midLS

rm test1 .test ..test	// rm nom_du_fichier : efface le fichier nom_du_fichier
rm -r test2		// rm -r nom_du_repertoire : efface le répertoire nom_du_repertoire (-r : recursive : remove directories and their contents recursively)